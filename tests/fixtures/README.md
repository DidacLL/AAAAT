# Test fixtures

Place reusable deterministic fictional inputs here. Fixtures must not contain real
profile data, employers, offers, recruiter messages, application documents,
workspace paths, or generated private artifacts.
