# Recovery — Owner-approved v2 direction

**Active:** [Issue #158](https://github.com/DidacLL/AAAAT/issues/158), based on the owner's accepted recovery plan. [OWNER_INTENT](../docs/OWNER_INTENT.md) defines meaning; [SPEC](../docs/SPEC.md) is the master architecture.

## Outcome and boundary

Reconcile the product definition, SPEC, skill and harness; retire the contradictory M6 journey. Correct verified external/provider identifier disclosure, inaccurate host-access guidance, raw-source retrieval and silent draft loss. Align the document engine contract with the owner's LaTeX2e/expl3/pdfTeX target.

Keep the useful desktop, service, sparse-information and portable-document foundations. Do not add compatibility machinery for pre-use development data. Required later capabilities remain in SPEC; this recovery does not declare the full document system, external-assistant context, configuration or information experience complete. Detailed blueprint/language design remains a later owner collaboration.

## Resume checkpoint — recovery is not complete

The recovery changes are on `agentic/masterplan-recovery`, originally implemented at `07954f0`; `881c531` added durable research rationale to SPEC. These refs identify evidence, not a requirement to repeat all checks after every documentation edit. Do not infer merge or completion from this file: inspect current Git/CI state.

**Implemented, awaiting acceptance:** owner definition/master architecture and harness reconciliation; separated provider/MCP wire identifiers; truthful host-access disclosure; full raw-source search projection; dirty-editor navigation guards; pdfLaTeX-only controls; a Windows `.cmd` fake compiler fixture. These are candidate corrections, not independently verified completion claims.

**Last local verification of the runtime changes:** typecheck and lint passed. Full Vitest reported failures in `test/latex-runner.test.ts` (`reports the non-zero latexmk exit code`) and `test/preload-api.test.ts` (`validates and forwards live candidature field operations over named IPC channels`). The run then produced no further output during subsequent polls and was interrupted. There is no complete suite result or established cause for its non-completion. Independent review and fresh packaged, visual and real-TeX evidence remain pending.

**Next bounded outcome:** diagnose and finish this recovery, then obtain independent review. Do not start the later capability destinations while declaring these unresolved checks complete. Review must cover:

- actual provider/MCP disclosure and field/choice/item/variant round trips, including stale/out-of-scope rejection; removing field IDs alone does not remove IDs stored as choice values;
- raw-only external creation and command/packaged-smoke fixtures after the changed wire contract;
- full source search and the new local `sourceSearchText` output in preload/test fixtures, without leaking it into external context;
- dirty editors across navigation, canceled workspace selection, settings, AI document content and adjacent actions;
- compiler tests using the intended fixture on Windows: the former extensionless fake could be bypassed by `cmd.exe` in favor of installed MiKTeX. A `.cmd` fixture was added, but that change does not prove every remaining runner failure has the same cause.

Run focused checks for corrections and the impact-selected suite. Diagnose failures without weakening assertions. Distinguish actual TeX/package execution from status acknowledgements. Classic is the next implementation/CI route; the owner transports an exact prompt. Use a separate Classic session for independent review. Request a bounded Terra task only for evidence GitHub/CI cannot supply.

Update this checkpoint as findings are resolved. Before replacing this Mission, carry any still-required unfinished capability into the SPEC/current Mission; never discard it merely because a new Issue or Mission starts. Keep enduring rationale in SPEC/ADRs, not this transient execution checkpoint.
## Evidence and continuation

Work starts from `84222de` on `agentic/masterplan-recovery`. GitHub records detailed execution and live integration state; the checked-in checkpoint below retains the information required to resume without this conversation or old PR comments. Run impact-selected verification, focused disclosure/retrieval/editing checks, and relevant TeX/packaged evidence. Independent review must challenge actual changes, not just a green suite.

The former M6 product journey is superseded. M0–M5 remain evidence of their stated technical foundations only. [ToDo Issue #156](https://github.com/DidacLL/AAAAT/issues/156) is a future capability proposal while this recovery is active; cached branches do not activate it.

After this capability is integrated, an orchestrator may activate one next bounded outcome from SPEC using actual implementation, evidence and live GitHub state. Routine activation needs no owner approval. Escalate consequential unresolved product meaning or Class D decisions only. Update this file to the active capability instead of appending a history or future Mission catalogue.
