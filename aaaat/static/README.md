# Static Assets

- `htmx.min.js` is the vendored htmx 2.0.4 minified distribution from `htmx.org` (`https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js`).

Do not replace it with a partial compatibility shim. If the version changes, update this note and the static-serving test.
