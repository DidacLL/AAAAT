name: Verify

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/**"
      - "src/**"
      - "test/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
      - "vite*.ts"
      - "forge.config.*"
      - "playwright.config.*"

permissions:
  contents: read

concurrency:
  group: verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Ordinary verification
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24.20.0
          cache: npm
      - run: npm ci
      - run: npm run verify

  gate:
    name: Verification gate
    needs: verify
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require ordinary verification
        env:
          VERIFY_RESULT: ${{ needs.verify.result }}
        run: |
          if [[ "$VERIFY_RESULT" != "success" ]]; then
            echo "Ordinary verification finished as $VERIFY_RESULT" >&2
            exit 1
          fi
          echo "Ordinary development verification passed." >> "$GITHUB_STEP_SUMMARY"
